# =====================================================================
# Claude-Code-Reals-SF-Leads-API - Non-secure configuration properties
# =====================================================================
# Secure values (salesforce.password, salesforce.securityToken) live in
# secure-config.yaml and are encrypted with the Mule Secure
# Configuration Properties module.
# =====================================================================

http:
  host: "0.0.0.0"
  port: "8081"

api:
  leadSource: "REALS"

salesforce:
  username: "CHANGE_ME"
  authUrl: "https://test.salesforce.com/services/Soap/u/53.0"
